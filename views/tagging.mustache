<!DOCTYPE html>
<link rel="stylesheet" href="../../stylesheets/tagging.css">
<div class="container">
		<div class="topTaggerWrapper">
		<form method="post" action="../tagging/limit">
		<button  type="submit" id="getNewMsg">Get New Messages</button>
			<div class = "limitWrapper">		
			<label for="limitSelector">Select how many messeges to get</label>
  			<select name="limit" class="customTaggingSelector">
				{{#limit}}
				<option value="{{n}}">{{n}}</option>
				{{/limit}}
  			</select>
			</div>
		</form>
		</div>
        	<div class="five columns alpha taggingMsg">
			<div class="messageContainer top">
				<p class="message">Messages</p>
			</div>

			{{#messages}}
				<div class="messageContainer {{pos}}">
        			<p class="messageText">{{text}}</p><p class="date">{{created_at}}</p>
				</div>
			{{/messages}}

		</div>
		<div class="ten columns omega fixedPosition">Put detailed info about the messeges here.</div>
</div>

